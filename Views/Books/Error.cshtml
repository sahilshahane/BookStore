@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Something went wrong!</h1>
<h2 class="text-danger">


    @(Model.Message == null || Model.Message.Length <= 0 ? "An error occurred while processing your request." : Model.Message)

</h2>